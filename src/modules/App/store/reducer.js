import { actions } from './actions';

const initialState = {
};

export default function appReducer(state = initialState, { type, payload }) {
  switch (type) {
    default:
      return state;
  }
}
